$kind: http-request
url: "{{baseUrl}}/api/v1/user/get-all"
method: POST
body:
  type: json
  content: |-
    {
        "page": 1,
        "limit": 10
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
